{
  "name": "upload-to-alioss-action",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "ali-oss": "^6.23.0",
    "fs-extra": "^11.3.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
